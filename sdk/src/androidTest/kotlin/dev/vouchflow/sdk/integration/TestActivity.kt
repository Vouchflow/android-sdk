package dev.vouchflow.sdk.integration

import androidx.fragment.app.FragmentActivity

/** Minimal host activity for BiometricPrompt in instrumented integration tests. */
class TestActivity : FragmentActivity()
