# Proguard rules for the SDK module itself.
# isMinifyEnabled = false for library builds, so these rules are not applied during library
# compilation. They are included here as documentation for maintainers.

# SDK does not minify itself — rules are in consumer-rules.pro for host app ProGuard passes.
